MonoBerryTester desktop app

This package provides the initial tool to test Mono router board

It contains:
- `main.py`: main program logic
- `texts.py`: all texts and message (so we can translate it later if needed)
- `styles.py`: UI styling constants

For more read the `docs.md` (generated documentation from code comments).