[project]
name = "monoberry-tester"
version = "0.1.0"
description = "Testing application for Mono Gateway Development Kit boards"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "PyQt5>=5.15.0",
    "requests>=2.31.0",
    "pyudev>=0.24.0",
]

[project.optional-dependencies]
dev = [
    "pylint>=3.0.0",
]